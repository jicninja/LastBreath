# Generated by scaffold-asset. Edit fields the scaffold did not know about (notes, importer_settings).
# Do NOT manually set approved, approved_at, approved_by, imported_hash, imported_at, target_unity_guid —
# those are populated by /art-approval-queue and by [MenuItem("LastBreath/Art/Import Approved Assets")].
# See docs/process/asset-design.md §5 for the full schema and rules.

id: {{ART_ID}}
name: {{name_kebab}}
purpose: {{PURPOSE}}

source: {{SOURCE}}
license: {{LICENSE}}
provenance:
  {{PROVENANCE_BLOCK}}

exports:
  - format: glb
    path: art/{{name_kebab}}/{{name_kebab}}.glb
    contains: [mesh, materials]

screenshots:
  - art/{{name_kebab}}/screenshots/preview-front.png
  - art/{{name_kebab}}/screenshots/preview-3q.png

approved: pending
approved_at: null
approved_by: null

imported_hash: null
imported_at: null
target_unity_path: {{TARGET_UNITY_PATH}}
target_unity_guid: null

importer_settings:
  scale_factor: 1.0
  generate_lights: false
  import_animations: false
  generate_colliders: false

notes: |
  TODO: open questions, exporter caveats, references.
