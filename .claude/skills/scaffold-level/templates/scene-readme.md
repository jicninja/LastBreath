---
id: {{SCENE_ID}}
type: scene
layer: gdd
status: poc
related: [{{LAYOUT_IDS}}]
---

# {{Scene Name}}

Source of intent: `docs/gdd/last-breath-poc-gdd.md#first-playable-scene` (and any later GDD updates).

## Goal

TODO: one paragraph — what player experience this scene must deliver. Reference pillars, not numbers.

## Layouts

This scene is composed of the following layouts. Each layout has an entry in `docs/registry/architecture.yaml` under `layouts:`. The layout YAML at `{{name_kebab}}.layout.yaml` is the audit artifact generated by `LevelSpec.DumpSpec` after every session.

- `{{LAYOUT_ID_0}}` — TODO: one-line description.
- `{{LAYOUT_ID_1}}` — TODO: one-line description.

## Open questions

- TODO: questions the level-designer should resolve with the game-designer or gameplay-programmer before the first session.

## Sessions

Session logs live under `sessions/`. Screenshots live under `img/`. See `docs/process/level-design.md` `§6` for the session log template.
